# Copy to .env for local development.

# Postgres — matches docker-compose.yml `db` service (published on host port 5433;
# 5432 is commonly taken by a locally-installed Postgres)
DATABASE_URL="postgresql://hyperchessuser:hyperchess@localhost:5433/hyperchess"
